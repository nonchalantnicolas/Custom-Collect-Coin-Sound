local shared = odh_shared_plugins
local section = shared.AddSection("Test Section")

section:AddLabel("Testing TextBox")
section:AddTextBox("Test Input", function(text)
    shared.Notify("You typed: " .. text, 3)
end, "")
section:AddButton("Test Button", function()
    shared.Notify("Button works!", 2)
end)
